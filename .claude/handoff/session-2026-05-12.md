# Session Handoff — 2026-05-12

## Việc đã làm trong session này

### 1. Chạy `/contact` trên file thực tế
Chạy contact-finder agent cho `data_leads_260805_cleaned.xlsx`:
- **31 công ty** được xử lý (Website != n.a, Contact Person == n.a)
- **29/31 công ty** tìm được ít nhất 1 contact
- **43 contact persons** đã điền — file tăng từ 36 → 50 dòng
- 2 công ty không tìm được: `WS Produktentwicklung GmbH & Co.` và `WSG Ingenieurgesellschaft mbH KFZ`
- Một số công ty có personal email rõ ràng: `sp@zpp.de`, `md@zpp.de` (ZPP Ingenieure AG)

### 2. Tái cấu trúc agents theo format 9 thành phần
Viết lại toàn bộ 4 agent files theo format chuẩn:

| Agent | Tools khai báo | Thay đổi chính |
|-------|---------------|---------------|
| `cleaner.md` | `[Glob, Read, Bash, Write]` | Thêm plan, self-eval 5 checklist, guardrails |
| `contact-finder.md` | `[Glob, Read, Write, WebFetch, WebSearch]` | Bảng dịch chức vụ Đức→Anh, xác nhận > 20 công ty |
| `tagger.md` | `[Glob, Read, Write, WebFetch, WebSearch]` | Phân nhóm A/B/C trước khi xử lý |
| `researcher.md` | `[WebFetch, WebSearch, Read, Glob, Grep]` | Viết lại từ đầu (từ 4 dòng → 9 thành phần) |

**Format 9 thành phần:**
1. Mục tiêu và vai trò
2. Bộ não suy luận
3. Prompt hệ thống / Policy
4. Bộ nhớ
5. Khả năng lập kế hoạch
6. Tool use
7. Feedback loop
8. Tự đánh giá / Kiểm tra chất lượng
9. Guardrails và an toàn

### 3. Quy tắc mới: Position ghi bằng tiếng Anh
- `contact-finder.md` có bảng dịch Đức → Anh đầy đủ (Geschäftsführer → Managing Director, Inhaber → Owner, Vorstand → Board Member, v.v.)
- `.claude/rules/contact-person.md` đã cập nhật: Position "dịch sang tiếng Anh theo bảng dịch" (trước đây: "giữ nguyên tiếng Đức")

**Lưu ý:** Các contact đã được tìm trong session này (43 rows trong file 260805) vẫn có thể có Position bằng tiếng Đức — cần review thủ công nếu cần chuẩn hóa.

### 4. Tách skill-specific content ra khỏi agents → vào skills

**Skills** giờ là **interface layer** (trigger + file selection + usage):
- `clean.md` — Glob *.xlsx, loại bỏ _backup_/_cleaned
- `classify.md` — Glob *_cleaned.xlsx, fallback *.xlsx
- `contact.md` — Glob *_cleaned.xlsx, fallback *.xlsx
- `report.md` — thêm trigger phrases
- `researcher.md` — **file mới tạo** trong session này

**Agents** giờ là **execution layer** — không còn đề cập `$ARGUMENTS` hay slash command.

### 5. Cập nhật CLAUDE.md
- `/contact`: status từ "⚠️ Chưa test" → "✅ Tested (31 công ty, 43 contacts)"
- Thêm `/researcher` vào bảng slash commands
- Cập nhật bảng Skills với cột "Nội dung skill"
- Cập nhật bảng Agents với cột `tools:` và mô tả format 9 thành phần
- Known gaps: xóa gap về `/contact` chưa test; cập nhật ngày từ 2026-05-08 → 2026-05-12

---

## Cấu trúc `.claude/` hiện tại

```
.claude/
├── agents/
│   ├── cleaner.md          (tools: Glob,Read,Bash,Write)
│   ├── tagger.md           (tools: Glob,Read,Write,WebFetch,WebSearch)
│   ├── contact-finder.md   (tools: Glob,Read,Write,WebFetch,WebSearch)
│   └── researcher.md       (tools: WebFetch,WebSearch,Read,Glob,Grep)
├── rules/
│   ├── output-schema.md    (alwaysApply: true)
│   ├── data-cleaning.md
│   ├── classification.md
│   ├── web-enrichment.md
│   └── contact-person.md   ← Position: tiếng Anh (cập nhật session này)
├── skills/
│   ├── clean.md            (interface: trigger + Glob *.xlsx)
│   ├── classify.md         (interface: trigger + Glob *_cleaned.xlsx)
│   ├── contact.md          (interface: trigger + Glob *_cleaned.xlsx)
│   ├── report.md           (interface: trigger + báo cáo spec)
│   └── researcher.md       ← mới tạo session này
├── handoff/
│   ├── session-2026-05-08.md
│   ├── session-2026-05-08b.md
│   ├── session-2026-05-08c.md
│   └── session-2026-05-12.md  ← file này
├── memory.md
└── settings.json
```

---

## Trạng thái hiện tại

| Thành phần | Trạng thái |
|-----------|-----------|
| `/clean` + Python pipeline | ✅ Hoạt động, đã test |
| `/classify` | ✅ Hoạt động, đã test 31 website |
| `/contact` | ✅ Tested — 31 công ty, 43 contacts (file 260805) |
| `/report` | ⚠️ Chưa báo cáo contact column coverage |
| `/researcher` | ✅ Skill mới, agent đã có logic đầy đủ |
| Agents (4 files) | ✅ Format 9 thành phần + tools khai báo |
| Skills (5 files) | ✅ Interface layer tách biệt khỏi agents |
| Rules (5 files) | ✅ contact-person.md: Position = tiếng Anh |

---

## Known gaps còn mở

1. **ZIP-from-City**: logic extract ZIP từ City (vd: "12345 Berlin") chưa có trong `src/normalizer.py`. Cần thêm `_extract_zip_from_city()` trước khi xử lý file raw mới.
2. **`/report`**: chưa báo cáo coverage 4 cột contact. Cần cập nhật `skills/report.md`.
3. **Position cũ tiếng Đức**: 43 contacts trong `data_leads_260805_cleaned.xlsx` được tìm *trước* khi có rule tiếng Anh — một số Position có thể vẫn là tiếng Đức. Review nếu cần.

---

## Việc cần làm session sau

- [ ] Thêm `_extract_zip_from_city()` vào `src/normalizer.py`
- [ ] Cập nhật `/report` để include contact column stats
- [ ] (Optional) Review và chuẩn hóa Position column trong file 260805 sang tiếng Anh
- [ ] Xử lý batch leads tiếp theo khi có file raw mới
