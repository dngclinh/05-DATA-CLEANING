# Session Handoff — 2026-05-08c

## Việc đã làm trong session này

### 1. Cải thiện CLAUDE.md (via `/init`)
- Thêm danh sách dependencies vào Setup: `pandas`, `openpyxl`, `phonenumbers`
- Thêm cột "Trạng thái" vào bảng slash commands (✅ Tested / ⚠️ Chưa test)
- Bổ sung quy tắc: email de-obfuscation (`[@]` → `@`), phone international format
- Thêm section **Known gaps** ghi lại 3 vấn đề chưa giải quyết

### 2. Refactor rules theo Claude Code rule specification
**Thêm frontmatter** vào 2 file hiện có:
- `.claude/rules/data-cleaning.md` — globs: `src/normalizer.py`, `excel_handler.py`, `tags.py`
- `.claude/rules/classification.md` — globs: `agents/tagger.md`, `src/tags.py`

**Tạo mới 3 rule files:**
- `.claude/rules/output-schema.md` — `alwaysApply: true` — schema 12 cột, n.a, yellow highlight, email/phone format
- `.claude/rules/web-enrichment.md` — globs: `agents/tagger.md` — thứ tự visit impressum/kontakt, field extraction rules
- `.claude/rules/contact-person.md` — globs: `agents/contact-finder.md` — senior titles, 1 row/person, contact email rules

**Cập nhật CLAUDE.md:** xóa sections đã chuyển vào rules, thêm bảng tham chiếu rules + agents.

### 3. Thảo luận automation (không implement)
Thảo luận nhiều phương án xử lý 3000 công ty mà không cần API key:
- Python scraper → user nói từng thử, không hiệu quả (block/JS/structure/accuracy)
- PowerShell loop `claude --print` → user nêu vấn đề token limit + usage limit
- Parallel multi-session (split chunks) → user reject ở bước ExitPlanMode
- **Kết quả: chưa chốt phương án, vấn đề automation vẫn còn mở**

### 4. Chuẩn hóa agents theo frontmatter spec
Thêm frontmatter `name / description / model: claude-sonnet-4-6` vào 4 agent files:
- `.claude/agents/cleaner.md`
- `.claude/agents/tagger.md`
- `.claude/agents/contact-finder.md`
- `.claude/agents/researcher.md` ← file mới, tạo trong session này

### 5. Tạo thư mục `.claude/skills/` với format mới
Tạo 4 skill files từ nội dung cũ trong `commands/`, dùng frontmatter `name / description`:
- `.claude/skills/clean.md`
- `.claude/skills/classify.md`
- `.claude/skills/contact.md`
- `.claude/skills/report.md`

### 6. Dọn dẹp files thừa
Đã xóa:
- `.claude/commands/` (toàn bộ — thay bởi `skills/`)
- `src/__pycache__/` (bytecode tự sinh)
- `data_leads_260805_backup_20260508_193922.xlsx` (backup thừa)

### 7. Cập nhật CLAUDE.md
- Sửa Known gaps: `commands/report.md` → `skills/report.md`
- Thêm section **Skills** với mapping skill → agent
- Thêm `researcher.md` vào bảng Agents

---

## Cấu trúc `.claude/` hiện tại

```
.claude/
├── agents/
│   ├── cleaner.md          (name: cleaner)
│   ├── tagger.md           (name: tagger)
│   ├── contact-finder.md   (name: contact-finder)
│   └── researcher.md       (name: researcher) ← mới
├── rules/
│   ├── output-schema.md    (alwaysApply: true)
│   ├── data-cleaning.md    (globs: normalizer, excel_handler, tags)
│   ├── classification.md   (globs: tagger.md, tags.py)
│   ├── web-enrichment.md   (globs: tagger.md)
│   └── contact-person.md   (globs: contact-finder.md)
├── skills/
│   ├── clean.md
│   ├── classify.md
│   ├── contact.md
│   └── report.md
├── handoff/
│   ├── session-2026-05-08.md
│   ├── session-2026-05-08b.md
│   └── session-2026-05-08c.md  ← file này
├── memory.md
└── settings.json
```

---

## Trạng thái hiện tại của project

| Thành phần | Trạng thái |
|-----------|-----------|
| `/clean` + Python pipeline | ✅ Hoạt động, đã test 36 công ty |
| `/classify` | ✅ Hoạt động, đã test 31 website |
| `/contact` | ⚠️ Chưa test với live data |
| `/report` | ⚠️ Chưa cập nhật cho 4 cột contact mới |
| Rules (5 files) | ✅ Đầy đủ frontmatter, đúng spec |
| Agents (4 files) | ✅ Đầy đủ frontmatter |
| Skills (4 files) | ✅ Tạo mới, thay thế commands/ |
| CLAUDE.md | ✅ Gọn, không duplicate rules |

---

## Known gaps còn mở

1. **ZIP-from-City**: logic extract ZIP từ City (vd: "12345 Berlin") chưa có trong `src/normalizer.py`. Cần thêm `_extract_zip_from_city()` trước khi xử lý file raw mới.
2. **`/report`**: chưa báo cáo coverage 4 cột contact. Cần cập nhật `skills/report.md` và logic đọc file.
3. **`/contact`**: chưa chạy thực tế. Test trước khi tin dùng.
4. **Automation 3000 công ty**: chưa có giải pháp — đây là việc ưu tiên cần quyết định session sau.

---

## Việc cần làm session sau

- [ ] Quyết định phương án automation cho 3000 công ty
- [ ] Test `/contact` với live data
- [ ] Thêm `_extract_zip_from_city()` vào `src/normalizer.py`
- [ ] Cập nhật `/report` để include contact column stats
