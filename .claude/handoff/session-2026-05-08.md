# Session Handoff — 2026-05-08

## Những gì đã hoàn thành

| # | Việc đã làm | Files thay đổi |
|---|-------------|----------------|
| 1 | Gộp `.claude/CLAUDE.md` vào root `CLAUDE.md`, xóa file cũ | `CLAUDE.md` |
| 2 | Auto-detect file: `/clean` và `/classify` tự list `.xlsx`, hỏi chọn thay vì đoán | `.claude/agents/cleaner.md`, `.claude/agents/tagger.md` |
| 3 | Thêm `WebFetch(*)` vào `settings.json` — classify không hỏi xác nhận từng website | `.claude/settings.json` |
| 4 | Change log → chỉ ghi khi data thực sự bị xóa; đổi tên sheet "Changes" → "Deleted Data" | `src/normalizer.py`, `src/main.py`, `src/excel_handler.py` |
| 5 | `/classify` nay tích hợp enrich: tự điền Phone, E-Mail, Street, City, Zip từ website | `.claude/agents/tagger.md`, `.claude/commands/classify.md` |
| 6 | Tạo agent + command `/contact` — tìm contact persons, mỗi người 1 dòng | `.claude/agents/contact-finder.md`, `.claude/commands/contact.md` |
| 7 | Thêm 4 cột mới vào template: Contact Person, Position, Contact Email, Contact Phone | `src/tags.py`, `CLAUDE.md`, `.claude/rules/data-cleaning.md` |

---

## Trạng thái hiện tại từng phần

| Module | Trạng thái | Ghi chú |
|--------|-----------|---------|
| `/clean` (Python script) | ✅ Hoạt động | Tested qua code review, chưa test với file thật |
| `/classify` (tagger agent) | ✅ Instructions đã cập nhật | Chưa test end-to-end với file thật |
| `/contact` (contact-finder agent) | ✅ Mới tạo | **Chưa test** — cần chạy thử với file thật |
| `/report` | ⚠️ Chưa cập nhật | Vẫn dùng định nghĩa cũ, chưa reflect 4 cột contact mới |
| Python code (`src/`) | ✅ Đồng bộ | tags.py + normalizer + excel_handler + main đã nhất quán |

---

## Quyết định quan trọng đã chốt

1. **Contact person = 1 dòng/người** — không dùng newline trong 1 ô, mỗi contact được thêm 1 dòng mới, data công ty copy lại
2. **Enrich gộp vào `/classify`** — 1 lần visit website, làm cả enrichment + classification (hiệu quả hơn)
3. **WebFetch cho phép tất cả domain** — `WebFetch(*)` vì domain công ty đa dạng, không thể whitelist
4. **Change log = safety net** — chỉ ghi khi data bị mất, không ghi formatting changes; sheet thường rỗng trong vận hành bình thường
5. **Impressum ưu tiên** — trang `/impressum` là nguồn đáng tin nhất cho công ty Đức (địa chỉ + phone + email)
6. **Contact email ≠ company email** — Contact Email chỉ lấy email cá nhân, không lấy info@/kontakt@

---

## Bước tiếp theo cần làm

### Ưu tiên cao
- [ ] **Test `/clean`** với file .xlsx thực tế — xác nhận Sheet "Deleted Data" hoạt động đúng
- [ ] **Test `/classify`** — xác nhận enrich + classify chạy đúng trên dữ liệu thật
- [ ] **Test `/contact`** — đây là feature mới nhất, cần test kỹ nhất

### Ưu tiên trung bình
- [ ] **Cập nhật `/report`** — thêm thống kê cho 4 cột contact (bao nhiêu công ty có contact, tỷ lệ tìm được, ...)
- [ ] Sửa mô tả trong CLAUDE.md: data flow vẫn ghi "8 columns" nhưng giờ là 12 cột

### Ưu tiên thấp
- [ ] Thêm `/report` vào danh sách agents trong CLAUDE.md Reference files
- [ ] Xem xét thêm LinkedIn vào nguồn tìm contact person (nếu website + Google không đủ)

---

## Những gì chưa làm xong

- **`/report` command chưa được cập nhật** — vẫn dùng logic cũ, chưa biết thống kê gì cho contact columns
- **Chưa có file thật để test** — toàn bộ session là thiết kế/cấu hình, chưa có lần chạy thực tế nào
- **Contact Phone trong enrich**: tagger.md có điền Contact Phone không? → chưa rõ, cần review lại tagger.md vs contact-finder.md để tránh overlap (tagger điền company Phone, contact-finder điền Contact Phone — hiện đã tách biệt, nhưng cần xác nhận)
